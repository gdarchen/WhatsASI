@startuml
title Diagramme de l'activité Comuniquer via chat V0.00

|Client|
start
:Envoi d'un message par l'utilisateur;
:sendMsg();
|#AntiqueWhite|Serveur|
:addMsgInList();
:broadcastMsg();
:aknowledgeMsgToSender();
|Client|
if (message bien transmis ?) then (yes)
:afficherMsg();
else(no)
:afficherErreur();
endif
stop
@enduml
